**20260904-自注意力机制在我们使用大模型时发生了什么？**

  

- **自注意力机制（Self-Attention）**让模型能够动态计算输入文本中每个词（Token）与其它所有词之间的关联程度。

- 发生了什么？

- Tokenization：你输入的问句被拆解成一个个词或字片段（称为 **Token**），并转化为数字向量。
- 寻找关联（Q、K、V 计算）和 计算**权重得分**：模型为每个词生成三个角色：**查询（Query, Q）**、键（Key, K）和值（Value, V）。模型用每个词的 Q 去匹配所有词的 K，计算出相似度分数。
- 加权求和输出，形成自注意力矩阵。

  

- 自注意力只是做了“信息的交换和聚合”（只是加权求和，属于线性变换），它缺乏复杂的逻辑推理能力。

  

- **前馈网络（Feed-Forward Network）** —— 每个词被独立处理以精炼其特征表示。

- **FFN 有两个线性层**，中间夹着一个激活函数。第一层扩展维度（通常到 4 倍），第二层将其收缩回原始大小。

- **把当前的词向量与无数个“知识模板”进行匹配**。

- **穿过 FFN 出来之后，它的向量里已经被强行注入了“巴黎”的特征。**

  

  

  

- 第 1 层的 FFN 层处理完后，输出的“新 Token 向量”并不会直接去生成答案。
- 它会作为输入，进入 **第 2 层** 的自注意力层。
- 在第 2 层里，它们**重新打招呼、换信息（此时交换的是已经带有初级知识的信息）**，然后再次独自进入第 2 层的 FFN 盖章。
- 接着是第 3 层、第 4 层……一直复制粘贴这个过程，直到穿过最后一层（比如第 32 层）。

  

  

  

  

- 当最后一层 Transformer 块处理完后，输入句子的**最后一个词（Token）**所对应的向量。
- 投影到一个巨大的向量空间，全人类词表空间
- 大模型根据这个概率分布进行**抽样（Sampling）**

  

  

  

  

  

  

- GPT-4 有几十层 Transformer 块，每一层都在重复：自注意力收集上下文 _→ FFN_ 提取深层语义。在实际应用中，所有的 Token 都是并行处理的（作为批处理矩阵乘法）
- 在标准的 Transformer 中，每个 Token 在每一层都会通过相同的前馈网络。但在混合专家模型（MoE）中，单一的前馈网络被多个前馈网络所取代 —— 每一个前馈网络都被称为一个**“专家（Expert）”**。
- 一个微型的**路由（Router）**网络决定了每个 Token 应该被发送到哪个（或哪些）专家。对于每个 Token，通常只有少数几个专家（例如 8 个或 64 个专家中的 2 个）会被激活。其余的保持闲置。

  

  

  

**把语序带来的逻辑关系，编织进了向量里**，再送给 FFN 去应用知识。

  

  

  

  

**留一个坑，**你是否清晰地看到了**残差连接**在这套数学引擎里的“纽带”作用？