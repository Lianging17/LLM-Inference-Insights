

1. **pytorch 是什么，为什么在大模型框架里被使用，解决了什么问题**

- 一个让计算机能够高效进行“张量计算 + 自动求导”的深度学习框架。
- Spring Boot 的核心是帮你组织业务系统

1. **在链路上的问题（通信）**

- 网络上：点击[www.baidu.com](http://www.baidu.com)发生了什么
- 在通信上：agent2agent发生了什么以及硬件之间的wifi和蓝牙是如何通信的
- AI应用上：语音上的输入如何直达天命？
- 结构基本一致：发送请求，协议封装，网络传输，目标解析，执行任务和返回结果

1. **遇到问题怎么办？**

- 定位问题

1. **RAG的召回是什么？**

- **召回（Recall）**是指从知识库中找出与用户问题相关的文档片段

- **向量召回（Vector Retrieval）**：将问题和文档转为嵌入向量，计算语义相似度，适合理解模糊意图但可能漏掉精准关键词。
- **稀疏/关键词召回（BM25）**：基于词频统计进行匹配，适合错误码、专有名词等精确查找，但缺乏深度语义理解。
- **多路召回（Hybrid/Multi-path Retrieval）**：结合向量检索、关键词检索和知识图谱等多种方式，取长补短以提高覆盖率。

- 优化召回效果：优化数据解析与切片（Chunking）、**调整评估指标**：使用 **Hit Rate@K**（正确证据是否在前K个结果中）和 **Context Recall**（所需证据是否找全）来量化效果。 

1. **模型微调是在做什么？**

- 预训练模型（如GPT、LLaMA），并在特定数据集上进一步训练，使其适应特定任务或领域。从“通才”变为“专才”。与提示工程相比，它能改变模型的行为/知识。
- **标准技术流程**

- **第1步：准备高质量数据集（决定成败的关键）**
- **第2步：选择微调方法**
- **第3步：**==**动手实操（可尝试）**==

6.Milvus

- 开源的高性能[**向量数据库**](https://milvus.io/docs/zh/overview.md)，专门用来处理人工智能和机器学习产生的海量非结构化数据。
- 核心功能:**向量检索**：快速找出语义最相似的图片、音频、视频或文本;海量存储;混合检索。

- 在传统数据库中，不加索引就会全表扫描，速度极慢。向量数据库也是如此。如果不建索引，Milvus 就需要用暴力计算（Flat）去算每一个向量的距离。
- 而且是决定系统**“检索速度”**和**“准确率（召回率）”**的最核心战场。
- HNSW 索引（基于图的索引）：把向量连成一张“高速公路网络图”。高层图是“高速路”（跨度大，跳跃快），底层图是“乡间小路”（跨度小，找得准）。检索时从高层一路往下跳跳跃，直到找到目标。

- 向量数据库

- 在RAG系统中，向量数据库是“外挂大脑”，用于存储和检索文本的向量表示。

- 核心使用步骤：构建索引、向量检索 (Search)
- 发展：过去是一个依赖库，数据全放在内存中。它不支持高可用。现在是一个真正的Database，可以在服务器上单独运行 Milvus，然后用任何语言的客户端去连接它——这和 MySQL、MongoDB 的架构完全一致。
- 典型架构：“电商图片搜索”系统中MySQL 负责存业务数据，Milvus 负责存图片特征，Milvus 里只记录：vector（向量数据）和 oss_image_id（图片ID）。用户搜索时，用户上传一张图片；系统把这张图片转成向量，去 **Milvus** 里搜索：“帮我找出最相似的 5 张图片的 ID”；Milvus 返回了 [101, 105, 203, 405, 502]；系统拿着这 5 个 ID 去 **MySQL** 里执行 SELECT * FROM products WHERE id IN (101, ...)，把商品的名称、价格、库存取出来，展示给用户。==“双库联动”==
- RAG架构

- 7.Embedding 的核心作用，就是把人类的非结构化数据，“翻译”成计算机能听懂的数学向量（一串浮点数数字），并且在这个过程中“保留数据的含义（语义）”。

- Embedding 模型并不是天生就懂人类语言的，它们的“语义理解”能力是通过**在海量文本中“连连看”和“猜词”**训练出来的。
- 核心的原理叫做 **“分布式假说”（Distributional Hypothesis）**：**上下文相似的词，其语义也相似。**
- Embedding 模型一旦选定，它的任务就很纯粹：把数据转成坐标。

- 8. re-ranking 重排逻辑是什么

- 在信息检索或 RAG（检索增强生成） 系统中，对初步筛选出的候选文档列表进行重新计算相关性得分并调整顺序的过程。
- 工作流程

- 1.**初筛（粗排）**：系统通过关键词匹配或向量嵌入（Embedding）从海量知识库中快速找出较多的候选结果（如 20~100 个），保证速度但精度一般
- **2. 精排（重排）**：使用计算开销更大、理解能力更强的 **Cross-Encoder（交叉编码器）** 模型，将“查询（Query）”和每个“候选文档（Document）”放在一起同时输入模型进行深度语义交互。

- 9.vLLM

- 最主流、最核心的开源大语言模型（LLM）高并发服务和推理引擎
- 单开一篇

- 10.关于做工程的疑问

- 1. 为什么如此流程化的东西需要面试？

- 答案是成本和速度的权衡（考虑到在工程上稳定性、成本和效果各方面的权衡）以及工程设计（答案里提到了融合两种检索的时候怎么去权衡两方的比重）
- 同时还可以考虑/扩展的问题是Milvus 存几千万条高维向量，内存可能直接爆掉。怎么优化？是降低向量维度、使用量化（Quantization）索引（如 SQ8），还是把部分非核心元数据移出向量库？

- 如果 Milvus 挂了，或者 Embedding 接口超时了，你的系统是直接报错，还是自动降级到传统数据库的 Elasticsearch（BM25 关键词检索）去撑住业务？
- 数据治理的脏活累活

- 11. hugging face为什么会有transformer库？

- 回答1是[**Hugging Face Transformers**](https://huggingface.co/docs/transformers/index) 是目前全球最流行、核心的**开源 AI 模型工具库**。它就像是 AI 领域的“通用软件播放器”。在过去，不同机构发布的 AI 模型（如 BERT、GPT、Llama）需要使用不同的专用代码才能运行。而 Transformers 库提供了一个**统一的 Python 代码框架**，让开发者用几行代码就能下载、运行和微调数万个顶尖的预训练模型。
- 回答2是

- 12. 为什么AI会产生幻觉

- **AI 本质上是一个“概率复读机”，而不是“事实数据库”**。底层逻辑是寻找一句话后面最可能接什么词。当遇到它不知道的知识时，为了把“接龙”顺下去，它会根据概率强行编造一个听起来极其通顺、符合语法，但内容完全虚构的答案。
- **数据源的污染与冲突**：AI 的训练数据来自整个互联网。互联网上充斥着小说、谣言、讽刺文学和错误信息。当这些矛盾的信息融在 AI 的“记忆”里，它就容易把虚构和现实混淆。

- 13. 什么是 Temperature（温度参数）？

- **Temperature（温度）** 是控制 AI 输出“创造力”或“随机性”的油门。
- 温度参数改变的，就是这个概率的分布形状。

- 14. base model

- **基座模型（Base Model）**：指经过大规模预训练，具备了广泛语言理解和生成能力的**基础模型**。它像一个“通才”，如**LLaMA、Qwen、DeepSeek**等

- 15.评估

- 模型评估的是**模型本身**的输出质量。
- RAG评估的是检索和生成两条链路。

- 16. **Function Calling（函数调用）**

- 让大模型**不直接回答**，而是**输出一段结构化的指令（JSON）**，由外部系统去执行这段指令。

- 17. 核心区别

- Function Calling是**1次交互**。问 -> 决定调用 -> 返回结果 -> 生成回答。**无状态**。
- 工作流 / Agent是**N次交互**。包含多个步骤，有前后依赖关系。**有状态**。整个流程共享一个上下文（Context），上一步查到的订单号，下一步直接用来退款。

- Billion是十亿单位
- LLaMA（Large Language Model Meta AI）是由 Meta 开发并开源的大型语言模型家族。（==可以了解硬件与部署要求和微调与推理方法==）

- LoRA（Low-Rank Adaptation，低秩自适应）是一种用于人工智能和大语言模型的高效轻量化微调技术，能以极低的成本让基础模型学会特定任务或风格。 ==（数学原理）==

- **冻结原模型**：使用LoRa时，大模型的原始参数（权重）全部被固定，不作任何改变，从而保留模型原有的通用能力。
- **外挂低秩矩阵**：它在原模型旁边添加一小部分被称为“低秩矩阵”的新参数。
- **高效训练**：训练时只更新这一小部分新增的参数，而不是调整整个模型成百上千亿的参数，使显存需求和计算时间大幅降低。

- Hugging Face TRL是一个专为大语言模型和 Transformer 架构设计开源的全栈训练与微调库。
- LLaMA-Factory是一个开源的[微调框架](https://zhida.zhihu.com/search?content_id=251396015&content_type=Article&match_order=1&q=%E5%BE%AE%E8%B0%83%E6%A1%86%E6%9E%B6&zhida_source=entity)
- **LlamaIndex**：是一个**框架/工具库**，它的名字里虽然有“Llama”，但**它不是模型**。它的专长是**连接私有数据到大模型**，通过构建向量索引来实现语义搜索和检索增强生成（RAG）。
- “我认为工程落地的核心是**用确定性的代码（工作流）去驾驭非确定性的模型（Function Calling输出）**。我的工作不是教模型做事，而是**给它画好围栏（校验器）和铺设好管道（状态流转）**，让它在可控范围内发散。”

  

  

待阅读

- [https://leetcode.wang/leetCode-68-Text-Justification.html](https://leetcode.wang/leetCode-68-Text-Justification.html)
- [https://windliang.wang/2026/08/26/%E6%97%85%E8%A1%8C%E5%8A%A9%E6%89%8B-Agent-%E5%AE%9E%E6%88%98%E4%B9%8B%E6%84%8F%E5%9B%BE%E8%AF%86%E5%88%AB/#more](https://windliang.wang/2026/08/26/%E6%97%85%E8%A1%8C%E5%8A%A9%E6%89%8B-Agent-%E5%AE%9E%E6%88%98%E4%B9%8B%E6%84%8F%E5%9B%BE%E8%AF%86%E5%88%AB/#more)
- [https://javaguide.cn/javaguide/contribution-guideline.html](https://javaguide.cn/javaguide/contribution-guideline.html)
- [https://jimmysong.io/zh/contact/](https://jimmysong.io/zh/contact/)

  

不懂的地方

- 距离的计算方法
- 数据并行、张量并行、流水线并行、序列并行和专家并行都是什么

  

可以实践

- [https://milvus.io/docs/zh/rerankers-overview.md](https://milvus.io/docs/zh/rerankers-overview.md)
- **在自己电脑上用GTX 3060（6G显存）跑通一次LLaMA-Factory的LoRA微调**
- **各种评测工具：**[**https://cloud.tencent.cn/developer/article/2731266?policyId=1004#1**](https://cloud.tencent.cn/developer/article/2731266?policyId=1004#1)
- 建议从**个人项目**开始，选择一个垂直领域（如金融、法律），用开源模型和工具（LLaMA-Factory, LangChain）完整地做一个项目，并把过程和结果量化出来。这会是进入这个领域最直接的敲门砖。

  

  

以后可能有用（也可能没用）的资料：

- https://github.com/ChinaYiqun/java-rag/tree/master