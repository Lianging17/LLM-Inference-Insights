
- "Pipeline"（流水线/管道）是一个通用概念，指**把一个任务拆分成多个连续处理阶段，数据依次流过这些阶段，每个阶段只负责一件事**。
- "Chunk"（分块/块）指**把一段较大的内容切分成较小的片段**
- "流式传输"（Streaming）指**数据不是一次性生成/传输完毕后再交给你，而是一边生成（或读取）一边分批次、连续地发送出去，接收方可以边收边处理**，不用等全部数据就绪。
	- 在 Anthropic API 里对应的就是设置 `stream: true`，返回的是一连串 SSE（Server-Sent Events）事件，而不是一个完整的 JSON。
	- YouTube、Netflix 播放视频时，不需要把整个视频文件下载完才能看，而是下载一部分播放一部分，边下边播。
