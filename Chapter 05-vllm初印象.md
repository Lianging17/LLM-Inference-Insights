

- **解决问题：**大模型生成文本是**逐字（Token-by-Token）生成**的，每多吐一个字，KV Cache 就会变大一点。系统必须在显存里开辟一块**极长的连续空间**。如果用户只聊了三句就退出了，后面剩下的连续显存就全部浪费了。

- **传统方法是**显卡用的是**“硬指针”**。它必须知道内存里一段死板、连续的物理地址，指到哪读到哪。

- 最主流、最核心的开源大语言模型（LLM）高并发服务和推理引擎
- **vLLM** 就是给大模型装上了“工业级超高压泵和智能分流器”，专门为了解决**高并发、低延迟、省显存**的线上生产工程问题
- career：https://vllm.ai/contact
- PagedAttention（分页注意力机制）：**KV Cache 离散地存储在不连续的显存块（Blocks）中**

- 重写了底层的 CUDA 算子（PagedAttention Kernel）。它允许 KV Cache 在显存里像碎积木一样**到处乱放**。
- **它做了一个“虚拟路由器”（页表）：** 记录着第 1 块在地址 A，第 2 块在地址 B。
- **欺骗 GPU：** 当 GPU 需要读取时，vLLM 的底层算子在硬件级别动态地把这些离散的地址映射成一个连续的序列，让 GPU 误以为它们是在一起的。在芯片内部**动态地把它们重新拼接成一个完整的、连续的临时矩阵**进行高速乘法计算。

- 在数据层面：数据一直都在显存（VRAM）里面，vLLM 只是在“改写显存里的指针地址（**逻辑地址**到物理地址的映射）”。
- **PagedAttention 解决了“空间（显存）怎么分”的问题，Continuous Batching 解决了“时间（GPU计算）怎么省”的问题。空间腾出来了，时间调度才能做到天衣无缝。**

  

  

  

  

**kv cache “以空间换时间”**

- 背景：当你准备算第 5 个词时，系统又要把 1、2、3、4、5 打包，**重新计算一遍 1、2、3、4 的内在特征**。每一次迭代都重新去计算它，就是对能量（算力）的纯粹浪费。我们在显存中开辟一块空间（Cache），当词语 1 第一次被计算时，把它的静态特征（K 和 V）永远锁死在内存里。算第 4 个词时，我们**只提取第 4 个词的特征**。然后去内存里把 1、2、3 已经准备好的特征直接拿过来做比对。
- 问题：都要把显存里长长的一条历史 KV 数据完整地读取、搬运到计算核心里去和新词做比对。系统瞬间变成了**带宽受限（Memory-Bound）**；随着对话越来越长，KV 缓存的体积呈线性暴增。
- 为什么会有kv cache?

- 大模型在生成文本时，是**一个字一个字（Token）往外蹦的**。更致命的是，它每生成一个新字，都必须把**之前所有的对话内容**（你的提问 + 它前面已经生成的字）全部重新读一遍，才能决定下一个字写什么。
- **由 Transformer 的核心内部结构决定的。** 准确地说，是由它的 **自注意力机制（Self-Attention）** 的数学公式直接决定的。

- 为什么放在显卡里？

- 速度对比：显卡 > 内存 > 固态硬盘
- 数据吞吐量（如果有 100 个用户同时在线，每秒钟 GPU 就需要吞吐 **数百 GB** 的 KV Cache 数据。）

- 在工程上是怎么把kv cache放在显卡里的？

- 在工程上，KV Cache 并不是一个独立存在的“外挂缓存”，它其实就是一堆普通的、由 PyTorch 统一管理的“多维数学矩阵”（Tensors）。
- 它在显卡里的存放、读取和更新，完全是通过显卡内存分配器（Cuda Memory Allocator）和指针切换来实现的。
- 过程分为三步（打开冰箱，装进大象，关上冰箱）：**静态显存分配**、**矩阵切片赋值**（GPU 算出了当前这一步的 K 矩阵和 V 矩阵。它们在显存中原本是作为临时变量存在的。工程代码（利用 PyTorch 等框架）会通过**显存指针**，直接把这两个临时矩阵的内容，复制到我们之前“圈好”的 KV Cache 大矩阵的对应位置上。）、**指针拼接（**算子直接把大矩阵中**该用户过去所有格子的显存首地址（指针）**传给 GPU 计算核心。GPU 顺着这些地址，直接在显存原地读取数据并进行乘法计算。）

- 技术实现：**PagedAttention (vLLM)**——打破连续内存限制，像操作系统管理硬盘一样，把 KV 缓存切成碎片物理存储，逻辑上连成一线，空间利用率直接拉满到 96% 以上。

  

  

  

- **计算的本质是“比对”（注意力机制的本质）**：在人类语言中，一个词的意思取决于上下文。这意味着，当模型处理到第 N 个词时，它**必须**把第 N 个词的信息，与前面第 1 到第 N-1 个词的信息全部“比对/交互”一遍。
- 任何计算系统的本质，都是在解决**信息传递与变换的效率问题**。

  

  

心生疑问：

- **Continuous Batching（连续批处理 / 动态批处理）**：传统的 Batching 需要等一个 Batch 里所有请求都生成完才能进行下一个。vLLM 允许“执行完的请求立刻退出，新请求立刻加入”，让 GPU 永远满载，不浪费一个时钟周期。为什么vllm可以同时管理GPU和显卡

- 这个是相对于static batching来说的，传统的方法是请求组成一个batch放在GPU计算。在这个方法里，传统系统把显存死死锁定了，请求全部完成内存才会被释放。 GPU 明明有庞大的计算能力，却只能围着最后一个人转，生产效率极低。
- kv cache掌控了

- **Tensor Parallelism（张量并行）** 和 **Pipeline Parallelism（流水线并行）是什么？**

  

careers:

- Kernels & Performance（算子与性能优化）: 显卡（GPU）就像跑车的超级引擎。大模型推理需要做几百亿次矩阵乘法（GEMM）。这些工程师的工作，是**直接用显卡听得懂的语言重写计算逻辑，让显卡在计算时一滴显存、一个马力都不浪费**。