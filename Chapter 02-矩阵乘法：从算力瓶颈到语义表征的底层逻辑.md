
## 1. 起始：为什么矩阵乘法值得被反复讨论？

在切入 AI 系统级研发之前，我一直在思考一个基础问题：大模型推理的底层瓶颈究竟在哪里？

第一章我们讨论了深度学习的本质——自动构建世界的表征。表征的载体是向量，而向量之间的运算，最终都收敛为矩阵乘法。但真正让我意识到矩阵乘法重要性的，是在 CPU 和 GPU 上分别跑不同尺度的矩阵乘法实验时的直观感受：**大模型推理中 Prefill 阶段和 Decode 阶段呈现出完全不同的硬件瓶颈形态。**

Prefill 阶段（一口气吞下提示词）：当你输入一句话（比如 1000 个字）并按下回车时，AI 必须同时把这 1000 个字全部读进去，并行计算它们彼此之间的注意力关系。这是一个**大矩阵乘法**，主要受限于算力（Compute-Bound）。

Decode 阶段（吐字，一次吐一个）：读完提示词后，AI 开始生成回复。每次只生成一个字，生成新字后把这个字加入输入，再吐下一个字。这是**小矩阵乘法**，但疯狂消耗显存带宽（Memory-Bound）。

这种对硬件行为的感知，正是我接下来去剖析 vLLM 调度优化和 PagedAttention 机制的理论基石。

## 2. 从 Java 开发到 AI 系统级研发：一次认知的跃迁

在传统的 Java 开发中，我们习惯于通过对象引用（Object Reference）和堆内存中的字段来组织业务逻辑。但在 AI 系统级研发中，世界的万物全部被抽象为由连续内存排布的 Tensor。

模型的每一层前向传播，本质上都是在进行大密度的矩阵乘法。这就是为什么理解数据排布（Contiguous）对速度的影响至关重要：在底层内存连续存储时，矩阵乘法为什么更快？这个问题的答案，将为未来接触 CUDA 内存对齐做铺垫。

**GEMM（General Matrix Multiply）** 的通用公式可以写成：
$$
C = \alpha(A \times B) + \beta C
$$
其中 $\alpha$ 和 $\beta$ 是系数。这个公式可以同时支持矩阵相乘、按比例缩放以及加上一个偏置矩阵，形式非常通用。深度学习中最常见的 $W \times X + b$ 操作，本质上就是 GEMM 的一个特例。

## 3. 语义的概念：从知识图谱到自注意力机制

早在 20 世纪 50 年代人工智能诞生之初，科学家们就一直在尝试让计算机理解“语义（Semantics）”。

**过去的知识图谱。** 早期的科学家认为，要让 AI 懂语义，就得把人类的知识编成一本巨大的“字典”。做法是科学家手动编写无数的逻辑规则和关系网（叫做知识图谱或本体论 Ontology）。比如教 AI：“苹果是一种水果”，“水果可以吃”。结果彻底失败。因为人类语言太复杂、变化太快，光是“意思意思”这四个字，靠人工写规则根本穷举不完。

**现在的 Transformer。** 词向量（Embedding）网络结构把每一个词都变成了一串包含几千个数字的坐标。含义相近的词，在空间里的距离就很近。自注意力机制（Self-Attention）是 Transformer 结构的灵魂，它能让 AI 根据上下文来决定一个词的精确含义：
$$
y = \mathrm{Attention}(Context)
$$

## 4. 维度跨越的本质：表征转换

通过一个权重矩阵 $W$，把一个低维/高维的输入向量，投影到另一个语义空间——这就是**表征转换**的核心操作。

这一思想贯穿了现代深度学习的整个演进路径。从自编码器（Autoencoder）的 Encoder-Decoder 范式，到 Transformer 的多头自注意力，本质上都是在做同一件事：**通过矩阵乘法实现表征空间之间的变换**。

## 5. BERT、GPT 与 Transformer：同源异构的三大架构

> **Transformer 是基础神经网络架构，而 BERT 和 GPT 是基于该架构衍生的两大不同方向的预训练模型。**

| 维度        | BERT                                   | GPT                                       | Transformer (Decoder-only) |
| --------- | -------------------------------------- | ----------------------------------------- | -------------------------- |
| **结构**    | 仅使用 Encoder                            | 仅使用 Decoder                               | 完整的 Encoder-Decoder        |
| **自注意力**  | 双向（每个词可见前后文）                           | 单向（仅可见前文）                                 | 多头自注意力                     |
| **预训练任务** | 掩码语言模型（MLM），随机挖掉 15% 的词做“完形填空”         | 自回归语言模型，根据前文预测下一个词（Next Token Prediction） | —                          |
| **擅长任务**  | 自然语言**理解**（NLU），如文本分类、情感分析、命名实体识别、问答匹配 | 自然语言生成（NLG），如文章创作、对话生成、文本续写、翻译            | —                          |

BERT 和 GPT 的分野，本质上是**表征学习的两种取向**：BERT 追求的是对上下文的深度理解（双向编码），GPT 追求的是对序列的精准预测（自回归生成）。而当前大模型推理优化的主战场，正是 GPT 所代表的 Decoder-only 架构。

## 6. Prefill 与 Decode：两种阶段的硬件瓶颈

回到开篇的实验观察。在 Decoder-only 架构的推理过程中，Prefill 和 Decode 两个阶段呈现出截然不同的计算特征：

- **Prefill 阶段(Compute-Bound)**：输入序列的各个位置可以并行计算，形成一个大的矩阵乘法，GPU 的数万个计算核心（Tensor Core）全部被塞满了数据，都在满负荷疯狂计算。此时限制速度的是 **GPU 每秒能做多少次浮点运算（TFLOPS，即算力上限）**。
- **Decode 阶段(Memory-bound)**：AI 已经生成了前 1000 个字，现在要生成第 1001 个字。对于 GPU 来说，当前的输入变成了 \(1 \times 1000\) 的**极小矩阵（行向量）**。每次只处理一个新 token，矩阵乘法的维度极小，但需要反复读取 KV Cache，此时限制速度的是**显存把数据传给芯片的速度（显存带宽，GB/s）**。显存带宽成为瓶颈，属于 **Memory-Bound**。

这一差异直接决定了推理框架的优化方向：Prefill 阶段需要压榨 GPU 的算力峰值，Decode 阶段则需要尽可能减少显存带宽的浪费。vLLM 的 PagedAttention 机制，正是针对 Decode 阶段的显存管理问题提出的解决方案。

## 后记

从 Java 的堆内存对象，到 GPU 显存中的连续 Tensor；从知识图谱的规则穷举，到自注意力机制的自动表征——矩阵乘法作为贯穿其中的底层运算，其重要性无论如何强调都不为过。

理解矩阵乘法的硬件行为，是理解大模型推理优化的第一步。接下来，我将继续深入 vLLM 的调度机制和 PagedAttention 的显存分页管理，逐步揭开大模型推理系统的全貌。