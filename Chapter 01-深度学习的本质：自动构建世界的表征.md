
**1.起始：我们为什么要讨论“表征学习”？**

![](https://my-obsidian-images-1485830040.cos.ap-shanghai.myqcloud.com//Users/scarlet/Documents/Obsidian%20Vault/imagesCleanShot%202026-09-11%20at%2013.54.13%402x.png)

这个问题起于看张小珺与谢赛宁访谈时产生的疑问：表征学习是什么？

抽象和形式化的任务对人类而言是最困难的脑力任务之一。AI 系统超越传统规则系统的核心，在于它具备**自己获取知识的能力**——即从原始数据中提取模式的能力，这就是机器学习。

但我一直在思考一个核心问题：**深度学习（Deep Learning）的核心贡献究竟是什么？** 它不仅仅是让机器学会了分类，它最伟大的贡献，是让机器能够自动构建“世界的表征”（Worlds' Representation，从而彻底解决了表示学习（Representation Learning）中的核心痛点。

**2.认识的跃迁：从人工特征工程（Feature Engineering）到工具自动化**

为了理解这种跃迁，可以对比我之前在研究生期间做过的**网络流量检测项目**与现代深度学习的差异：

- **传统机器学习时代（以网络攻击流量识别为例）**：  
    在复杂的网络环境中，为了识别混入安全流量中的攻击信号，课题耗费大量精力做 **特征工程（Feature Engineering）**。我需要深刻理解网络协议、流量如何发生、在网络中获取什么数据。通过人工提取指标或指标组合，去支撑下一步的结论。**如果没有前期对网络领域的深厚知识，这条研究路径根本无法成立。**
- **深度学习时代的降维打击**：  
    深度学习改变了我们认识和理解数据的形式——从“人”转移到了“工具/机器”。即便是一个没有网络背景的人，借助深度学习也能识别攻击流量。

在 ImageNet 的经典工程中，这种能力体现得淋漓尽致：神经网络自动学习特征。当输入一只猫的图片像素时: 深度学习通过由较简单概念间的联系定义复杂概念、从一般抽象概括到高级抽象表示，解决了“人类难以用像素直接描述车轮/猫脸”的难题。

**3.解构复杂度的度量：模型的深度究竟在描述什么？**

	如何定义一个“解决问题的复杂度”？我们可以从两个系统级维度来衡量：
	1. 计算路径长度：从输入到输出的最长计算路径长度。
	2. 概念关联图的深度：将描述概念彼此如何关联的图的深度，视为模型的深度。

最典型的代表就是**自编码器（Autoencoder）**。它由编码器（Encoder）和解码器（Decoder）组合而成。编码器将输入数据转换为一种不同的低维/稠密表示，解码器再将其重构。我们期望在压缩与重构的过程中尽可能多地保留信息，并在这个“瓶颈”中逼机器压榨出数据最核心的、具有良好特性的表征。这种 Encoder-Decoder 范式，成为了后续诸多系统级架构（包括 Transformer、VAE 等）的基石。

**4.落地演进：从分布式表征（Embedding）到大火的 RAG 技术**

表征（Representation）的本质，是实现 **原始数据 → 向量 → 概念** 的跨越。而 **嵌入（Embedding）** 则是表征的一种高级形式——**分布式表征（Distributed Embedding）**。它将人类语言、图像或者其他离散数据，映射为计算机能够处理的连续、稠密向量。

在近两年大热的 **RAG（Retrieval-Augmented Generation，检索增强生成）** 架构中，Embedding 扮演了核心角色：
- **传统搜索**：基于字面的硬匹配（Hard Matching），缺乏对语义的理解。
- **基于表征学习的 RAG**：Embedding 成了连接自然语言与机器能理解的数据语言的桥梁。我们通过**语义相似度（如余弦相似度）**进行模糊查找，只要语义相近，加上适配的检索机制，生成的结果就更为准确。

**RAG 的大行其道，本质上完全建立在表征学习的基础之上。** 而构成这一深度学习核心的，正是底层的神经网络、反向传播算法以及大规模数据的工程化吞吐。

-----

**后记**

在 RAG 和 LLM 时代，世界的表征变成了海量的 Token 和 KV Cache（键值缓存）。当模型层数变深、参数变大时，如何高效地在 GPU 显存中存储、传输和计算这些表征，成为了新的系统瓶颈。这也是 vLLM 等推理框架需要引入 PagedAttention（显存分页表征管理）的核心原因。有些概念是我感兴趣的点，慢慢填坑～