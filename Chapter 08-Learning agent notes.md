**Source:**

```python
@misc{hello_agents2025,
  title  = {Hello-Agents: Building an AI Agent from Scratch},
  author = {Sizhou Chen and Tao Sun and Shufan Jiang and Peilin Huang and Xinmin Zeng and Hao Hu and Xinzhong Zhu and all Hello-Agents contributors},
  year   = {2025},
  url    = {<https://github.com/datawhalechina/Hello-Agents>},
  note   = {GitHub repository}
}
```

**Quote:**

1. 如今 Agent 构建主要分为两派，一派是 Dify，Coze，n8n 这类软件工程类 Agent，其本质是流程驱动的软件开发，LLM 作为数据处理的后端；另一派则是 AI 原生的 Agent，即真正以 AI 驱动的 Agent。
    
2. 在人工智能领域，智能体被定义为任何能够通过**传感器（Sensors）**感知其所处**环境（Environment）**，并**自主**地通过**执行器（Actuators）**采取**行动（Action）**以达成特定目标的实体。
    
    真正赋予智能体"智能"的，是其**自主性（Autonomy）**。智能体并非只是被动响应外部刺激或严格执行预设指令的程序，它能够基于其感知和内部状态进行独立决策，以达成其设计目标。这种从感知到行动的**闭环**，构成了所有智能体行为的基础。
    
3. 在过去设想智能家居的场景：
    
    最简单的**反射智能体（Simple Reflex Agent），**它们的决策核心由工程师明确设计的“条件-动作”规则构成。经典的自动恒温器便是如此：若传感器感知的室温高于设定值，则启动制冷系统。
    
4. 一种经典的混合架构是分层设计：底层是一个快速的反应模块，处理紧急情况和基本动作；高层则是一个审慎的规划模块，负责制定长远目标。而现代的 LLM 智能体，则展现了一种更灵活的混合模式。它们通常在一个“思考-行动-观察”的循环中运作，巧妙地将两种模式融为一体：
    

- **规划(Reasoning)** ：在“思考”阶段，LLM 分析当前状况，规划出下一步的合理行动。这是一个审议过程。
- **反应(Acting & Observing)** ：在“行动”和“观察”阶段，智能体与外部工具或环境交互，并立即获得反馈。这是一个反应过程。

1. **亚符号主义 AI（Sub-symbolic AI）**

在这里，知识并非显式的规则，而是内隐地分布在一个由大量神经元组成的复杂网络中，是从海量数据中学习到的统计模式。神经网络和深度学习是其代表。

1. 这个循环主要包含以下几个相互关联的阶段：

- **感知 (Perception)**：这是循环的起点。智能体通过其传感器（例如，API 的监听端口、用户输入接口）接收来自环境的输入信息。这些信息，即**观察 (Observation)**，既可以是用户的初始指令，也可以是上一步行动所导致的环境状态变化反馈。
- **思考 (Thought)**：接收到观察信息后，智能体进入其核心决策阶段。对于 LLM 智能体而言，这通常是由大语言模型驱动的内部推理过程。如图所示，“思考”阶段可进一步细分为两个关键环节：
    - **规划 (Planning)**：智能体基于当前的观察和其内部记忆，更新对任务和环境的理解，并制定或调整一个行动计划。这可能涉及将复杂目标分解为一系列更具体的子任务。
    - **工具选择 (Tool Selection)**：根据当前计划，智能体从其可用的工具库中，选择最适合执行下一步骤的工具，并确定调用该工具所需的具体参数。
- **行动 (Action)**：决策完成后，智能体通过其执行器（Actuators）执行具体的行动。这通常表现为调用一个选定的工具（如代码解释器、搜索引擎 API），从而对环境施加影响，意图改变环境的状态。


**Reflection:**

1. 从局限性出发，“状态”（state）的概念开始被引入，用来描述环境中被观测对象或事件的情况，记录其发生的变化。由此发展出了基于模型、基于目标和基于效用的智能体。这些智能体所倚赖的核心 —— 规则、模型和效用函数，都来自于人类设定的先验知识。而学习型智能体（Learning agent)的核心思想是不依赖预设，通过与环境的互动自主学习指导下一步。实现学习这一过程最具代表性的是**强化学习（Reinforcement Learning, RL）。**
2. GPT 的出现让“通用推理引擎”第一次变得实用，从而让 Agent 从学术概念变成了工程产品。传统智能体是显式编程和知识构建，行为确定且有边界。而LLM智能体通过海量数据的预训练，获得隐式的学习和判断能力，从而以更加灵活的方式处理更为通用的任务。

