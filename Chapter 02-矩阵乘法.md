
- **算力受限（Compute-Bound）与带宽受限（Memory-Bound）的直观感受**：  
	在 CPU 和 GPU（若有）上分别跑不同尺度的矩阵乘法，记录耗时，感受大模型推理中 **Prefill 阶段（大矩阵乘法，吃算力）** 与 **Decode 阶段（逐字生成，小矩阵乘法，狂吃显存带宽）** 的底层逻辑。
- **数据排布（Contiguous）对速度的影响**：  
	展示在底层内存连续存储时，矩阵乘法为什么更快，为未来接触 CUDA 内存对齐做铺垫。
- 和Java对比
	- “在传统的 Java 开发中，我们习惯于通过对象引用（Object Reference）和堆内存中的字段来组织业务逻辑；但在 AI 系统级研发中，世界的万物全部被抽象为了由连续内存排布的 Tensor。模型的每一层前向传播，本质上都是在进行大密度的矩阵乘法（GEMM）。通过这个小实验，我直观感受到了大模型推理中 Prefill 和 Decode 两个阶段完全不同的硬件瓶颈（算力受限 vs 带宽受限）。这种对硬件行为的感知，正是我接下来去剖析 vLLM 调度优化和 PagedAttention 机制的理论基石。”
- 矩阵乘法 GEMM （**Ge**neral **M**atrix **M**ultiply）
	- 公式$$C = \alpha(A \times B) + \beta C$$这里的 $\alpha$ 和 $\beta$ 是系数。这个公式可以同时支持矩阵相乘、按比例缩放以及加上一个偏置矩阵（类似于深度学习中的 $W \times X + b$），形式非常通用。
- 语义的概念
	- 早在 20 世纪 50 年代人工智能诞生之初，科学家们就一直在尝试让计算机理解“语义（Semantics）”。
	- 过去的知识图谱
		- 早期的科学家认为，要让 AI 懂语义，就得把人类的知识编成一本巨大的“字典”。
		- **做法**：科学家手动编写无数的逻辑规则和关系网（叫做**知识图谱**或**本体论 Ontology**）。比如教 AI：“苹果是一种水果”，“水果可以吃”。
		- **结果**：彻底失败。因为人类语言太复杂、变化太快，光是“意思意思”这四个字(太好笑了🤭)，靠人工写规则根本穷举不完。
	- 现在的transformer
		- **词向量（Embedding）**：网络结构把每一个词都变成了一串包含几千个数字的坐标。含义相近的词，在空间里的距离就很近。
		- **自注意力机制（Self-Attention）**：这是 Transformer 结构的灵魂。它能让 AI 根据**上下文**来决定一个词的精确含义。$$y = \text{Attention}(\text{Context})$$
	- 维度跨越的本质（表征转换）
		- 通过一个权重矩阵 \(W\)，把一个低维/高维的输入向量，投影到另一个语义空间（体验空间变换）。
- BERT、GPT和Transformer的核心
	- ==**Transformer是基础神经网络架构，而BERT和GPT是基于该架构衍生的两大不同方向的预训练模型。**==
	- 在结构上
		- **Transformer**：包含完整的编码器（Encoder）和解码器（Decoder），采用多头自注意力机制。
		- **[BERT](https://zhuanlan.zhihu.com/p/69290203)**：只使用了Transformer的编码器（Encoder）部分。它的自注意力机制是双向的，每个词都可以同时看到前文和后文。
		- **GPT**：只使用了Transformer的解码器（Decoder）部分。
	- 预训练任务
		- **BERT**：采用 **掩码语言模型（MLM）**，即随机挖掉句子中15%的词，让模型结合上下文来做“完形填空”。
		- **GPT**：采用传统的 **自回归语言模型**，任务是根据前文 **预测下一个词**（Next Token Prediction）。
	- 擅长任务
		- **BERT**：擅长 **自然语言理解（NLU）**，如文本分类、情感分析、命名实体识别、问答匹配等。
		- 
- Transformer（Decoder-only）
	- 1.不同于BERT，
	- 2. Prefill 阶段和Decode阶段
		- **Prefill 阶段（一口气吞下提示词）**：当你输入一句话（比如 1000 个字）并按下回车时，AI 必须**同时**把这 1000 个字全部读进去，**同时且并行地**计算它们彼此之间的注意力关系。
		- **Decode 阶段（吐字，一次吐一个）**：读完提示词后，AI 开始生成回复。它每次只生成**一个字**。生成新字后，把这个字加入输入，再吐下一个字。
	- 3.