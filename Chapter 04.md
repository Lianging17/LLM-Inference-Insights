**20260904-重新理解硬件**

  

- **GPU（图形处理器）** 确实相当于**计算中心**

  

- 执行大模型推理时，GPU在做什么？

- 权重参数（Weights）：模型的“大脑”（比如 70B 或 8B 的参数文件），在模型启动时就已经**死死地固定在显卡的显存（VRAM）中**了。
- KV Cache（键值缓存，Key-Value Cache）：它们是对话过程中的“临时记忆”。比如大模型读了顾客 A 说的上一句话，要把这句话的中间计算结果（KV）存起来，免得生成下一个字时重新算一遍。

- 运行时数据的流动：

- **加载启动**：当你启动 AI 软件时，CPU 会把大模型文件从**硬盘**读出来，通过主板上的通道（PCIe 通道），一股脑地**运送到显卡的“显存”里**。
- **常驻显存**：运进去之后，模型参数就驻扎在显存里不动了。这时候显卡就变成了一个独立的“数据中心”。
- 缓存（Cache）：当 GPU 要计算某一个字时，它会把显存里的一小撮 KV Cache 数据“抓”到芯片内部的这个小抽屉里，算完之后再放回去。

- 硬件如何被软件操控

- 在显卡里塞了几千个甚至上万个微小的计算核心（CUDA Core），每个核心就像一个**只会做加减乘除的“小算盘”**。硬件还规定了数据从显卡内存（显存）运到这些算盘里的**“传送带”（高速总线）**。
- **软件部分（随便改的）**：这几万个算盘同时开工，必须要有一本**“操作手册”**。这本手册规定了：算盘 A 第一步算什么、算盘 B 什么时候去传送带上接数据、怎么把大家算出来的结果汇总。**这本操作手册，在显卡里就被称为 Kernel（算子/内核）。**

- 显卡读取数据的整个完整工程链路

- **第0步（冷启动）**：大模型的几百 G 参数文件（权重）最早是在电脑的**硬盘（SSD）**里的。
- **第1步（CPU 搬运）**：你的 Python 业务代码启动，**CPU** 发出指令，把模型参数从硬盘读到电脑的**系统内存（RAM）**中。
- **第2步（GPU 显存初始化）**：CPU 通过 PCIe 通道（高速公路），把这些参数一股脑灌进显卡的**物理显存（VRAM，就是显卡上的内存条）**里。
- 当用户发送了一句：“你好，请问...”，真正的推理计算开始了。
- **CPU 下达动工令**：CPU 把用户的这句话转成向量，存入显存。然后 CPU 给 GPU 核心发送一个信号：“兄弟们，开工！去执行 vLLM 写的 PagedAttention 算子。
- **GPU 核心（工头）拿到指针和映射表**：
- GPU 核心于是发出硬件读指令，顺着 32 号和 85 号地址，把数据从显存（VRAM）拉取到 GPU 芯片内部的超级缓存（SRAM/寄存器）里。

  

  

- CPU 怎么知道该用什么算子？

  

  

- 硬件存储的本质

- 在**显存**里，数据是一群**疯狂求生的电荷**；
- 在**固态硬盘**里，数据是被**关在绝缘监狱里的电子**；
- 在**机械硬盘**里，数据是无数个**指向不同方向的微型指南针**。