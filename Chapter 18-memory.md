## 模块 8：Memory

### 技术选型

| 记忆类型 | 存储方案 | 金融场景用途 |
|---|---|---|
| 短期对话记忆 | Redis / 内存 | 当前对话上下文 |
| 任务状态记忆 | LangGraph Checkpointer + Postgres | 断点续跑、回放 |
| 长期语义记忆 | 向量库（Qdrant/pgvector） | 历史研究结论、用户偏好 |
| 结构化记忆 | PostgreSQL / MySQL | 标的池、组合、关注列表 |
| 情景记忆 | 向量库 + 时间戳 | 某次研究的完整过程 |
| 摘要记忆 | LLM 压缩 + 存储 | 长对话压缩，省 token |

框架选型：

```
Mem0 → 开箱即用，适合快速集成
Zep → 时序感知，适合金融时效场景
Letta（原 MemGPT）→ 分层记忆，研究友好
自研 → Postgres + Redis + 向量库，最可控
```

**金融推荐：自研分层记忆，因为时效、权限、审计要求高。**

### 工程上可以展示的细节

**1. 分层记忆架构**

```
┌─────────────────────────────┐
│ 工作记忆（当前任务 State）    │ ← LangGraph State
├─────────────────────────────┤
│ 会话记忆（当前对话历史）      │ ← Redis，TTL 24h
├─────────────────────────────┤
│ 长期记忆（跨会话）           │
│  ├─ 语义记忆：研究结论、偏好  │ ← 向量库
│  ├─ 结构化记忆：标的池、组合  │ ← Postgres
│  └─ 情景记忆：历史研究过程    │ ← 向量库 + 时间戳
└─────────────────────────────┘
```

**2. 记忆的写入策略**

不是所有对话都值得记。写入触发条件：
- 用户明确说“记住这个”
- 出现新的标的研究结论
- 用户偏好变化（如“我偏好价值投资”）
- 任务完成后的摘要

写入前做**冲突检测**：新记忆和旧记忆矛盾时，标记冲突，让用户确认或按时间优先。

**3. 记忆的检索策略**

- 按相关性（向量检索）+ 时效性（时间衰减）+ 重要性（LLM 打分）综合排序
- 金融场景时效权重高：3 个月前的估值结论可能已失效
- 检索时带过滤条件：只召回该用户有权限的记忆

**4. 时效性处理**

金融数据有强时效性。工程上：
- 每条记忆带 `timestamp` 和 `expire_at`
- 过期记忆不删除，但标记为“历史”，检索时降权
- 财务数据记忆绑定 `report_period`，新报告期发布后旧记忆自动降级

**5. 权限隔离**

金融场景多用户，记忆必须隔离：
- 用户级记忆：只有本人可见
- 团队级记忆：团队共享
- 公司级记忆：全公司共享
- 检索时强制带权限过滤

### 面试角度：为什么这样做？

**1. 为什么需要 Memory？**

> “LLM 本身无状态，上下文窗口有限。Agent 任务跨多轮、跨会话，需要记住用户偏好、标的、中间结论。金融场景还要记住：数据时效、历史研究、避免重复拉数据。Memory 分短期、长期、结构化、情景四类，各自用不同存储。”

**2. Memory 和 RAG 什么区别？**

> “RAG 是检索外部知识，Memory 是检索自身历史。RAG 的数据源是文档、数据库，Memory 的数据源是对话、任务状态、历史结论。技术上两者都用向量检索，但 Memory 更强调时效、权限、冲突处理。”

**3. 记忆冲突怎么办？**

> “三层处理：一是写入时检测冲突，标记矛盾；二是按时间优先，新记忆覆盖旧记忆；三是金融场景关键结论让用户确认。比如用户之前说‘关注茅台’，后来说‘不关注了’，系统要更新偏好而非并存。”

**4. 金融场景的记忆时效怎么处理？**

> “每条记忆带时间戳和过期时间。财务数据记忆绑定报告期，新报告发布后旧记忆降权。检索时用时间衰减函数，越新的记忆权重越高。过期的记忆不删除，标记为历史，支持回溯。”

**5. 怎么评估 Memory 的效果？**

> “看三个指标：记忆召回率（该记的是否记住）、记忆准确率（检索出的记忆是否相关）、任务完成率（有 Memory vs 无 Memory 的对比）。金融场景额外看时效准确率：是否用了最新数据。”

