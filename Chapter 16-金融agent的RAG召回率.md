**RAG召回率**（Context Recall / Recall）指的是==在RAG（检索增强生成）系统中，所有真正与用户问题相关的知识或文档里，有多少比例被成功检索（召回）了出来==。 

**核心概念与计算方式**
- **一句话概括**：有没有把用户真正需要的知识全部找到，关注的是“全不全”，而不是“排得对不对”。
- **计算公式**：  
- **评估意义**：如果召回率太低，意味着关键信息被漏掉了，大模型在后续的生成阶段巧妇难为无米之炊，容易导致回答不全面甚至“一本正经地胡说八道”（幻觉）。


**提高召回率的具体方法**
核心目标就是**确保所有与用户问题相关的切片（Chunks）都能被成功检索到**。
- 文本分块与增强优化（数据准备阶段）
	- **分块优化 (Chunk Size)**：调整 Chunk 的大小。Chunk 太小容易丢失上下文，Chunk 太大则信息密度低。通常采用**带有重叠（Overlap）**的切分方式（如 Chunk 512 token，Overlap 50 token），防止关键信息落在切分边界而被漏掉。
	- **小数据块与大上下文检索 (Parent-Child / Small-to-Big Chunking)**：在数据库中对小段文本（如单句）进行向量索引以保证检索的精确度，但在命中后，**召回其所属的整段甚至整篇文档（大上下文）**传给大模型。
	- **元数据过滤 (Metadata Filtering)**：在文档上打上标签（如时间、类别、作者、地区）。检索时先用元数据进行硬过滤（Filter），在缩小的范围内再进行相似度检索，防止不相关的噪声挤占了有效召回名额。
- 问句改写与拓展（Query 处理阶段）
	- **查询重写 (Query Rewriting)**：利用 LLM 将用户的口语化问句转换为更标准、更适合搜索引擎检索的表达方式。
	- **查询扩展 / 衍生 (Query Expansion / Multi-Query)**：利用 LLM 针对用户的同一意图生成 **3-5 个不同角度的同义问句**，分别去数据库检索，最后合并去重。这能极大提高查全率。
	- **假设性文档 embeddings (HyDE - Hypothetical Document Embeddings)**：让 LLM 先根据用户问题“盲猜”生成一个虚构的、理想的答案（尽管可能包含错误事实），然后**用这个虚构答案的向量去检索**真实文档。这比用简短的问题去检索长文档更匹配。
- 多路检索与混合检索（检索算法阶段）
	传统的单一向量检索在处理关键词精确匹配时效果较差，需要多技术结合。
	**混合检索 (Hybrid Search)**：将 **向量检索（稠密检索 - Dense）** 与 **传统关键词检索（稀疏检索 - BM25）** 结合。
	向量检索**：捕捉长文本和语义层面的相似度（哪怕词语不一致，语义相近也能找出来）。**BM25 检索**：确保专有名词、产品型号、货号、错误码等高精准关键词不漏掉。


增大检索数量与重排（后处理阶段）
- **放大 Top-K**：在初筛阶段，将原本只取 5 个切片的限制**放大到 Top-20 甚至 Top-50**。虽然这会带来噪声，但能把潜在的相关文档都“捞上岸”。
- **重排机制 (Reranking)**：作为放大 Top-K 的配套操作。使用轻量级的重排模型（如 BAAI/bge-reranker）对这 50 个切片进行精细的语义相关度打分，再**截取前 5-10 个真正最相关的**喂给大模型。这样既保证了高召回，又过滤了噪声。
  
量化和测试 RAG 系统的召回率（Context Recall）
- **RAGAs（RAG Assessment）** 框架
- 完整过程
	- 构建测试数据集（Evaluation Dataset）
		你无法在整个数据库上凭空测召回率，必须先准备一个具有代表性的**黄金测试集**（建议包含 50 - 200 个问题）。
	- 核心量化指标：Context Recall 的计算逻辑
		- 在测试时，你把测试集的问题输入系统，记录系统捞上来的 `Retrieved Contexts`。然后利用一个强模型（如 GPT-4o）作为裁判，去比对**标准答案**与**检索到的切片**

```
【任务描述】
请分析给定的“标准答案（Ground Truth）”和“检索到的上下文（Retrieved Context）”。
1. 将标准答案拆解为独立的、原子级的事实陈述。
2. 对于每个事实陈述，判断它是否可以从“检索到的上下文”中推导/找到。
3. 如果能找到，标记为 [YES]；如果找不到，标记为 [NO]。

【输入】
问题: 公司的年假政策是什么？
标准答案: 全职员工入职满一年可享受 5 天带薪年假，且年假必须在当年内休完。
检索到的上下文: 公司为全职员工提供福利。入职满一年后，员工每年享有 5 天的带薪假。

【LLM输出示例】
- 事实 1: 全职员工入职满一年有 5 天带薪年假。 -> [YES]（在上下文中提及）
- 事实 2: 年假必须在当年内休完。 -> [NO]（上下文中漏掉了这一条）

最终得分: 1 / 2 = 0.5 (召回率为 50%)

```


```
from ragas import evaluate
from ragas.metrics import context_recall
from datasets import Dataset

# 1. 准备你的测试数据（运行你的 RAG 检索模块来收集 retrieved_contexts）
data_samples = {
    'question': ['如何修改账户密码？', '你们的退款政策是什么？'],
    'contexts': [
        ['用户可以点击右上角设置，进入安全中心修改密码。'], # 实际检索到的切片
        ['收到商品后7天内支持无理由退货，但特价商品除外。'] 
    ],
    'ground_truth': [
        '在个人设置的安全中心里可以修改密码。',            # 真实标准答案
        '普通商品7天内可退，但特价商品不享受退换服务。'
    ]
}

dataset = Dataset.from_dict(data_samples)

# 2. 调用 RAGAs 评测（默认需要配置 OpenAI API Key 作为裁判）
score = evaluate(dataset, metrics=[context_recall])

# 3. 输出量化结果
df = score.to_pandas()
print(df[['question', 'context_recall']])
print(f"系统平均召回率: {df['context_recall'].mean():.2f}")

```