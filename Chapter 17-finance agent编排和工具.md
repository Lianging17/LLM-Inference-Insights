**1.为什么选 LangGraph 而不是 CrewAI？**
> “CrewAI 适合快速搭一个角色扮演式的 demo，但金融场景需要显式控制流程、可审计、可回放。LangGraph 把流程定义为图，每个节点做什么、什么条件下跳转，都是代码里写死的，LLM 只在节点内做局部决策。这满足金融合规对可解释性和可复现性的要求。”

**2. 为什么 Agent 不是简单 Prompt？**

这是面试高频题，参考回答框架：

> “简单 Prompt 是单轮、无状态、无工具、无规划的。Agent 需要：目标分解（Planner）、工具调用（Tool Calling）、状态管理（State）、记忆（Memory）、反馈循环（Validation）、护栏（Guardrails）。金融场景还要加上可审计和可回放。这六个能力缺一个，都不能算生产级 Agent。”

**3. State 怎么设计？**

> “用 TypedDict 定义严格 schema，包含：用户输入、任务 DAG、中间结果、已调用工具列表、数据来源、错误信息、当前节点。所有节点读写同一个 State，保证信息不丢失。金融场景额外加 `data_version` 和 `timestamp` 字段，用于审计和时效校验。”

**4. 怎么防止 Agent 无限循环？**

> “三层防护：一是在图层面限制最大迭代次数（LangGraph 的 recursion_limit）；二是在条件边里加显式终止条件（比如‘校验通过’或‘达到最大重试次数’）；三是在节点内加成本追踪，超过 token 预算就强制终止。”

**5. 多 Agent 怎么通信？**

金融场景推荐 **Supervisor 模式**：主 Agent 负责任务分解和调度，子 Agent 只负责执行自己的子任务并返回结果。子 Agent 之间不直接通信，所有信息通过 State 流转。好处是链路清晰、易审计、易调试。


### 面试角度：为什么这样做？

**1. Tool Calling 怎么实现？**

> “模型根据工具 Schema 输出 tool_call，运行时解析参数、校验、执行，把结果作为 tool message 返回模型，循环直到最终回答。关键在工程细节：JSON Schema 校验、权限检查、超时重试、幂等、沙箱、日志。协议上 OpenAI function calling 最通用，MCP 正在成为工具接入标准。”

**2. 为什么不直接让 LLM 算财务指标？**

> “LLM 做算术不可靠，尤其是多步计算和单位换算。金融场景一个数字错了，整个结论就错了。所以数值计算必须外置到 Python 沙箱，LLM 只负责选择用哪个工具、解释结果。这也是金融 Agent 和通用 Agent 的核心区别。”

**3. 工具调用失败怎么办？**

> “分类型处理：参数错让 LLM 重试，超时重试加降级，限流退避，数据源挂了切备源。核心原则是：宁可不回答，也不能编数据。金融场景错误的数字比没有数字更危险。”

**4. MCP 是什么？为什么重要？**

> “MCP 是 Anthropic 提出的 Model Context Protocol，标准化了模型和工具之间的接口。好处是工具一次接入，多个模型/Agent 都能用。金融场景数据源多，MCP 能降低接入成本。目前还在早期，但趋势明显。”

**5. 怎么保证工具调用安全？**

> “沙箱隔离、权限控制、参数校验、审计日志。金融场景还要做合规检查：比如禁止调用内幕信息相关工具，禁止生成投资建议。所有调用可回放，满足监管要求。”